include: package:very_good_analysis/analysis_options.10.3.0.yaml

linter:
  rules:
    # Toléré pour un projet MVP — les docs d'architecture sont en Markdown
    # parallèle plutôt que dans le code.
    public_member_api_docs: false
